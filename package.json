{
  "name": "talent-iq",
  "packageManager": "yarn@3.5.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "build": "yarn workspace backend install && yarn workspace frontend install && yarn workspace frontend build",
    "start": "yarn workspace backend start",
    "dev": "concurrently \"yarn workspace backend dev\" \"yarn workspace frontend dev\""
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "engines": {
    "node": "^22.0.0"
  }
}
